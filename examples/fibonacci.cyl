# Implementation of the Fibonacci sequence in Cylium

func fib(n: int) -> int
    if n <= 1
        return n
    end

    return fib(n - 1) + fib(n - 2)
end

func main() -> int
    int n = input() as int

    for i from 0 to n
        echo(fib(i))
    end

    return 0
end