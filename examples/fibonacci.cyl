# Implementation of the Fibonacci sequence in Cylium

func fib(n: number) -> number
    if n <= 1
        return n
    endif

    return fib(n - 1) + fib(n - 2)
end

func main() -> number
    number n = input() as number

    for i from 0 to n
        echo(fib(i))
    endfor

    return 0
end