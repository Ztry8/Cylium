# Sieve of Eratosthenes in Cylium

func main() -> number 
    number n = input() as number
    bool[] p = [n + 1; true]

    p[0] = false
    p[1] = false

    for i from 2 to n
        if p[i]
            number j = i * i 
            while j <= n 
                p[j] = false
                j += i
            end
        end
    end

    for i from 2 to n 
        if p[i]
            echo(i)
        end
    end

    return 0
end